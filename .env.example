# SCANNER credentials (read-only IAM user: aivar-scanner)
# Used by main.py, dashboard, and verify_test_misconfigs.ps1
AWS_ACCESS_KEY_ID=AKIA...
AWS_SECRET_ACCESS_KEY=...
AWS_DEFAULT_REGION=ap-south-1

# LLM - Groq (preferred, free tier) / Grok / OpenAI
GROQ_API_KEY=gsk_...
GROQ_MODEL=llama-3.3-70b-versatile

# GROK_API_KEY=xai-...
# OPENAI_API_KEY=sk-...

# Level 3 — Slack escalation + SQLite persistence
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...
DB_PATH=storage/findings.db

# ADMIN credentials go in .env.admin (NOT here) — see .env.admin.example
# Admin user: aivar-admin with AdministratorAccess — setup scripts only
