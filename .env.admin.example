# ADMIN credentials — for creating test misconfigs ONLY
# Copy to .env.admin (never commit .env.admin)
# Do NOT put these in .env — keep .env as aivar-scanner read-only keys

AWS_ACCESS_KEY_ID=AKIA...admin-key...
AWS_SECRET_ACCESS_KEY=...admin-secret...
AWS_DEFAULT_REGION=ap-south-1
